#include <stdio.h>
#include <string.h>

int main(void)
{
/*initializiing strings*/
char str[] = "\"Programming is like building a multilingual puzzle";

/*writing to stdout*/
puts(str);
return (0);
}
