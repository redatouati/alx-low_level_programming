#include <string.h>
/**
* _strlen - returns the length of a string
* @*s: pointer to char, the string
*
* Return: int
*/
int _strlen(char *s)
{
	/*int n = 4;*/
	int length;
	
	length = strlen(s);

	return (length);
}
