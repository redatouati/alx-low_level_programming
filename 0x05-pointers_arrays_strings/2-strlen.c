#include <string.h>
/**
* _strlen - returns the length of a string
* @s: the string to return length from
*
* Return: int
*/
int _strlen(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
